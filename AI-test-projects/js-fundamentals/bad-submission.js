// ============================================
// BAD SUBMISSION - JS Fundamentals
// ============================================

// No functions implemented correctly

function reverseString(str) {
  console.log(str); // just logs, doesn't return reversed string
}

function isPrime(n) {
  return n > 1; // completely wrong logic
}

// Missing isPalindrome, getUniqueValues, countCharacters

console.log("done");
reverseString("hello");
isPrime(4); // returns true incorrectly
